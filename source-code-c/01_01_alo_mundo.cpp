#include <stdio.h>  /* inclusão do arquivo cabeçalho (header)
                       da biblioteca padrão de entrada e saída (stdio) */
#include <stdlib.h> /* inclusão do arquivo cabeçalho (header)
                       da biblioteca padrão (stdlib) */
int main() {

    printf("Alo Mundo!!!!\n");

    system("pause");
    return 0;
}    
